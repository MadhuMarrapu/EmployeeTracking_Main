server:
  port: ${PORT:8080}

spring:
  application:
    name: Employee-Tracking-System
  datasource:
    url: jdbc:postgresql://localhost:5432/emp-track-test
    username: postgres
    password: Postgres1996@@

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Kolkata
    show-sql: true

  jackson:
    time-zone: Asia/Kolkata

  main:
    allow-circular-references: true

  data:
    jpa:
      repositories:
        bootstrap-mode: lazy
